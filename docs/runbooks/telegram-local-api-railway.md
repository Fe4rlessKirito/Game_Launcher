# Historical Railway Telegram Bot API runbook

Railway is no longer the active Vaultnode staging target. The current private
Telegram Local Bot API deployment is documented in
`docs/runbooks/telegram-local-api-mantle.md` and uses the Mantle VPS Docker
Compose stack.

The Railway files remain in the repository only as historical/configuration
fallback material; do not use them for the active staging deployment.
