name = "vaultnode-mail-ingest"
main = "src/index.ts"
compatibility_date = "2026-08-11"
workers_dev = false
preview_urls = false

[vars]
PROVISIONING_INGEST_URL = "https://gamelauncher-production.up.railway.app/internal/v1/email-events"
DEBUG_FORWARD_ENABLED = "false"
DEBUG_FORWARD_ADDRESS = ""

# Configure PROVISIONING_INGEST_URL as a non-secret deployment variable and
# INGEST_HMAC_SECRET with `wrangler secret put`.
